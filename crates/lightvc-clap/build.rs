// build.rs — clap-wrapper integration
// The wrapper's build.rs handles VST3/AUv2 SDK compilation automatically.
fn main() {
    // clap-wrapper's own build.rs runs via [build-dependencies]
    // No additional configuration needed here.
}
